name: My first workflow
on: push
jobs:
    first_job:
        runs_on: ubuntu-latest
        steps:
            - name: welcome message
              run: echo "my first workflow"
    
            - name: list file
              run: ls
            
            - name: Read file
              run: cat README.md
